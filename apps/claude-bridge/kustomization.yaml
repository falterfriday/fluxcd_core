apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: claude-bridge
resources:
  - namespace.yaml
  - rbac.yaml
  - certificate.yaml
  - networkpolicy.yaml
  - service.yaml
  - deployment.yaml
configMapGenerator:
  - name: claude-bridge-app
    files:
      - bridge.py
