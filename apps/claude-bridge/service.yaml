---
apiVersion: v1
kind: Service
metadata:
  name: claude-bridge
  namespace: claude-bridge
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: claude-bridge
  ports:
    - name: http
      port: 8080
      targetPort: http
