---
extends: default

ignore: |
  .github/

rules:
  line-length: disable
  indentation: disable
  comments-indentation: disable
  document-start:
    level: warning
  truthy:
    check-keys: false
  braces:
    max-spaces-inside: 1
  empty-lines:
    max: 2
